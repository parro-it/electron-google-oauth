/*
let moduleRoot = '../es6';
if (process.env.TEST_RELEASE) {
  moduleRoot = '../dist';
}

const electronGoogleOauth = require(moduleRoot);

describe('electronGoogleOauth', () => {
  it('works', async () => {

  });
});

*/
